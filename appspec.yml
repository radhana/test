version: 0.0
os: linux
files:
  - source: /index.html
    destination: /var/www/html/
hooks:
  BeforeInstall:
    - location: deloy/before_install
      timeout: 300
      runas: ubuntu
  AfterInstall:
    - location: deloy/restart_server
      timeout: 300
      runas: ubuntu
